'use strict';

describe('app.add module', function() {

  beforeEach(module('app.add'));

  describe('add controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('AddCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});